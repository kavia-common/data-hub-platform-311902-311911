# This FastAPI backend uses in-memory storage for tests and does not require environment variables.
# Intentionally left mostly empty to keep configuration explicit and avoid hardcoding secrets.

# Optional:
# SITE_URL="http://localhost:3000"
